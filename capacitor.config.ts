import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.crm1',
  appName: 'crm1',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
